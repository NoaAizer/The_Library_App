<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thelibrary">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logolab"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:logo="@drawable/logolab"
        android:theme="@style/Theme.TheLibrary">
        <activity android:name=".activities.LocationActivity"></activity>
        <activity android:name=".activities.SearchResultActivity" />
        <activity android:name=".activities.SearchBookActivity" />
        <activity android:name=".activities.CompletesOrdersActivity" />
        <activity android:name=".activities.BookDetailsActivity" />
        <activity android:name=".activities.ReturnBookActivity" />
        <activity android:name=".activities.inventoryListActivity" />
        <activity android:name=".activities.deliveryListActivity" />
        <activity android:name=".activities.TAListActivity" />
        <activity android:name=".activities.OrderPageActivity" />
        <activity android:name=".activities.TreatmentOrdersActivity" />
        <activity android:name=".activities.MyOrderActivity" />
        <activity android:name=".activities.ListOfBorrowedBooksActivity" />
        <activity android:name=".activities.UserDetailsActivity" />
        <activity android:name=".activities.BooksListAdminActivity" />
        <activity android:name=".activities.BooksListUserActivity" />
        <activity
            android:name=".activities.MenuAdminActivity"/>
        <activity
            android:name=".activities.MenuUserActivity"
            android:label="@string/title_activity_menu_user"/>

        <activity
            android:name=".activities.LoginAdminActivity"
            android:label="@string/title_activity_login_admin" />

        <activity
            android:name=".activities.RegisterUserActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.TheLibrary.NoActionBar" />
        <activity
            android:name=".activities.AddBookActivity"
            android:exported="true">
    </activity>

        <activity android:name=".activities.LoginUserActivity" />
        <activity android:name=".activities.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>